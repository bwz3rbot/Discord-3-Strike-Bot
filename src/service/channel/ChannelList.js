const // A list of all the channels defined in this package.
    general = require('./General');
    tagme = require('./Tag-Me');
const channels = [
    general,
    tagme
]
exports.channels = channels